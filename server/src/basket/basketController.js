import basketService from "./basketService.js"

class BasketController {
    async create(req, res) {
        
        // const result = await basketService.createBasket(req)

        // return res.json(result)
    }   
}

export default new BasketController();