export type RegisterModalinputs = {
    fullname: string;
    email: string;
    password: string;
    checkbox: boolean;
};

export type inputsBskDevice = {
    size: string;
    color: string;
    count: number;
}

export  type LoginModalFormInputs = {
    email: string;
    password: string;
}